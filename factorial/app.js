let number = Number(prompt("Plz enter a Number : "));
let calculate = 1;
for(let i = 1; i <= number; i ++)
{
    calculate = calculate*i;
}
alert("you are calculating number of factorial which you write : " + calculate);
